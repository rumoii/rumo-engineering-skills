# Page Patterns

Use the target application's existing patterns as the authority. Compare at least one adjacent page with the same interaction shape before introducing a new layout.

Common patterns to verify include list and filter pages, create/edit forms, detail drawers or dialogs, tabs, dashboards, import/export flows, and long-content scrolling. For each pattern, preserve loading, empty, error, permission, disabled, validation, and responsive states.
